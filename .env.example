# .env Example File

# --------------------------------------------------------------------------
# Application Environment & Debugging
# --------------------------------------------------------------------------
# APP_ENV: Set to 'development', 'testing', 'production'
# APP_DEBUG: Set to 'true' for development to show detailed errors.
#            Set to 'false' for production.
# APP_URL: The base URL of your application (e.g., http://127.0.0.1:8000)
# APP_TIMEZONE: Default timezone for your application (e.g., "UTC", "Africa/Johannesburg")

APP_ENV=development
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000
APP_TIMEZONE=UTC

# --------------------------------------------------------------------------
# Database Configuration
# --------------------------------------------------------------------------
# DB_CONNECTION: Driver (e.g., 'mysql', 'pgsql', 'sqlite')
# DB_HOST: Database host (e.g., 127.0.0.1)
# DB_PORT: Database port (e.g., 3306 for MySQL, 5432 for PostgreSQL)
# DB_DATABASE: Database name
# DB_USERNAME: Database username
# DB_PASSWORD: Database password
#
# --- Example for SQLite (using DB_PATH) ---
# DB_CONNECTION=sqlite
# DB_PATH="var/database/src.db" # Path relative to ROOT_PATH for SQLite

# --- Example for MySQL/MariaDB ---
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=school_management_db
DB_USERNAME=root
DB_PASSWORD=K5575081m.

# --- Using DB_DSN (takes precedence if set in web/index.php PDO setup) ---
# Example DSN for SQLite (absolute path recommended for consistency)
# DB_DSN="sqlite:/var/www/myproject/var/database/src.db"
# Example DSN for MySQL
# DB_DSN="mysql:host=127.0.0.1;port=3306;dbname=my_framework_db;charset=utf8mb4"


# For the current setup, we're using DB_PATH for SQLite default in web/index.php
# If you want to use MySQL, uncomment the MySQL lines and comment out/remove DB_PATH
DB_PATH=storage/database/app.db


# --------------------------------------------------------------------------
# View Engine Configuration
# --------------------------------------------------------------------------
# VIEW_ENGINE: Choose 'twig' or 'plates'. Defaults to 'twig' if not set (as per etc/view.php)
# TWIG_CACHE_ENABLED: Set to 'false' to disable Twig caching, otherwise it uses
#                     the path defined in etc/view.php. (e.g., 'true' or 'false')
VIEW_ENGINE=plates
# Set to 'false' in development if you don't want caching during template changes
# TWIG_CACHE_ENABLED=false

# --------------------------------------------------------------------------
# SessionManager Configuration
# --------------------------------------------------------------------------
# SESSION_DRIVER: (e.g., 'file', 'database', 'redis' - framework currently uses PHP native 'file' sessions)
# SESSION_LIFETIME: Lifetime of the session in minutes (PHP's default is usually 1440 seconds / 24 minutes)
# SESSION_COOKIE_NAME: Name of the session cookie (e.g., MY_APP_SESSID)

# These are typically managed by PHP's session etc (php.ini) or session_start options.
# Our SessionManager class uses some defaults like HttpOnly, SameSite.
# SESSION_COOKIE_NAME=MYAPPSESSIONID # Example if you want to customize it via SessionManager options

# --------------------------------------------------------------------------
# Mail Configuration (if using PHPMailer or similar)
# --------------------------------------------------------------------------
# MAIL_MAILER: (e.g., 'smtp', 'sendmail', 'mail')
# MAIL_HOST: SMTP host
# MAIL_PORT: SMTP port (e.g., 587, 465, 25)
# MAIL_USERNAME: SMTP username
# MAIL_PASSWORD: SMTP password
# MAIL_ENCRYPTION: (e.g., 'tls', 'ssl')
# MAIL_FROM_ADDRESS: Default "from" email address
# MAIL_FROM_NAME: Default "from" name

MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=99232b4834e629
MAIL_PASSWORD=9451c832619ac8
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@example.com
MAIL_FROM_NAME="${APP_NAME} Mail" # Example using another env var

# --------------------------------------------------------------------------
# Maintenance Mode
# --------------------------------------------------------------------------
# MAINTENANCE_MODE: 'true' to activate, 'false' or empty to deactivate.
# ALLOWED_MAINTENANCE_IPS: Comma-separated list of IPs allowed to bypass maintenance.
# MAINTENANCE_BYPASS_TOKEN: A secret token to bypass maintenance via query param (optional).

HEADERS_X_POWERED_BY_ENABLED=true

MAINTENANCE_MODE=false
# ALLOWED_MAINTENANCE_IPS="127.0.0.1,::1,YOUR_DEVELOPMENT_IP"
# MAINTENANCE_BYPASS_TOKEN=your_secret_bypass_key

# --------------------------------------------------------------------------
# Other Custom Application Settings
# --------------------------------------------------------------------------
# APP_NAME="My PHP Framework" # Example
CORS_ALLOWED_ORIGINS=http://127.0.0.1:8000

CACHE_DRIVER=apcu

JWT_SECRET=hWLnIhQxgiihugfZCWX+pg+uwWExnvKrq5rFqZMh0Ss=
JWT_EXPIRATION=3600
